apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-microservice
spec:
  replicas: 1
  selector:
    matchLabels:
      app: users-microservice
  template:
    metadata:
      labels:
        app: users-microservice
    spec:
      containers:
        - name: users-microservice
          image: docker.io/library/users-microservice
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: ASPNETCORE_ENVIRONMENT
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: ASPNETCORE_ENVIRONMENT
            - name: JWT_SigningKey
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: JWT_SigningKey
            - name: NEON_DB_PASSWORD
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: NEON_DB_PASSWORD
---
apiVersion: v1
kind: Service
metadata:
  name: users-microservice
spec:
  selector:
    app: users-microservice
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: LoadBalancer
